pipeline {
    agent any                 // niente container
    stages {
        stage('Build') {
            steps { echo '🔨 Compilazione OK' }
        }
        stage('Test') {
            steps { echo '✅ Test OK' }
        }
        stage('Notify') {
            steps {
                script {
                    if (env.BRANCH_NAME == 'master') {
                        echo '🚀 PRODUZIONE'
                    } else {
                        echo '🔧 TEST'
                    }
                }
            }
        }
    }
}
