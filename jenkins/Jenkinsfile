pipeline {
    agent any
    triggers {
        cron('0 3 * * *')
    }
    stages {
        stage('Nightly Build') {
            steps {
                echo 'Build at 3 AM'
            }
        }
    }
}
